#include<iostream>
using namespace std;
int main() {
    // // float can store real number 
    // float x =  3.443;
    // float y = 2.342432;
    // cout<<x+y<<endl;

    // cout<<5/2<<endl; // both 4 & 5 are int so the result will be int 
    // cout<<5.0/2<<endl; // 5 is not int its float so the result will be float 
    // cout<<5/2.0<<endl;  // 2 is not int its float so the result will be float 
    // cout<<5.0/2.0;
    
    // int v = 8;
    // float k = 9.6;
    // cout<<v+k;

    // question 
    // devident = devisore * qutoitent + remainder

    // int a , b;
    // cin>>a; 
    // cin>>b;
    // int x = a / b; 
    // cout<< "Remainder = "<< a - (b * x) <<endl;

    // cout << 2%4<<endl; // this happens because 4 is < than 2 so the remainder will 2 


    bool y = 0;
    cout<<y;
    return 0;

}